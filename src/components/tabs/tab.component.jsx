import React from 'react';

const Tab = ({
    index,
    title,
    extraClass,
    event,
    children,
}) => {
    return (
        <>
            {children}
        </>
    );
};

export default Tab;